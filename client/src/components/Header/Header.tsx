import styled from "styled-components"

const Container = styled.div`
  background-color: rgba(188,50,48,255);
`

export const Header = () => {
  return (
    <Container>
      Header
    </Container>
  )
}